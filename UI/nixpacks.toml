[phases.setup]
nixPkgs = ["nodejs_22"]
nixpkgsArchive = "nixos-unstable"

[phases.install]
cmds = ["npm install --engine-strict=false"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve -s build -l 3001"

[variables]
PORT = "3001"